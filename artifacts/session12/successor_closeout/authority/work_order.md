# Session 12 Semantic Contract Foundry Successor Work Order

This public-safe work order governs an additive successor to the historically closed Session 12 implementation. Historical Session 12 artifacts remain byte-preserved and authoritative only for their original implementation. Until this successor closes, its operational status is `GO_SESSION_13:NO`.

The successor freezes source bytes before semantic processing, accounts for every source span or structured node, records statement-level authority, enforces stateless Standard and Deep stage order, maps frozen semantic artifacts to exact immutable candidate surfaces after the blind freeze, derives a bounded verification plan, and projects only proposals into Session 11 owner disposition.

Public real use, six semantic stability runs, and semantic self-dogfood precede the final implementation and policy freeze. One authoritative unpublished `0.5.1` wheel is then built and fresh-installed. At least six isolated hidden holdouts are frozen only by a protected evaluator after the wheel exists. Their one-shot qualification uses deterministic hard gates and two blind arm-independent semantic adjudicators. A material hidden failure or later semantic production repair closes the successor partially with `GO_SESSION_13:NO`.

Raw source bundles are private-local and cleanup-bound. Hidden cases, adjudication, private strategy, confidential scanner inputs, and planning/execution authority remain outside Git. Public artifacts contain only bounded requirements, aggregate categories, digests, limitations, and non-sensitive evidence.

The exact normative claims are the 97 entries in `claim_registry.v1.public.json`. Its self-declared digest is recomputed over the canonical registry without the digest field. No claim may be omitted, renumbered, weakened, or closed by unrelated evidence.

Session 13, target execution, challenge execution, Enterprise capability, release, tag, and PyPI publication remain out of scope.
