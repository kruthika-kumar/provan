# AI evaluation guidance v1

Fixed inputs, oracles, pass conditions, version bindings, execution results, deterministic validation, and production traces are separate evidence stages. Product-outcome measurement never substitutes for AI behavior evaluation, and an AI eval never proves product impact.
